{
  "name": "npm-package-suggest",
  "version": "1.0.0",
  "description": "api to get suggestion of NPM package when the one you want is taken",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "npm",
    "package",
    "suggestion"
  ],
  "author": "Jorge Florian",
  "license": "ISC",
  "dependencies": {
    "npm-registry-client": "^8.6.0",
    "promisify-node": "^0.5.0"
  }
}
